(()=>{const engine=()=>window.MyTrainingProgression;function enhance(){const e=engine(),title=document.querySelector('.session-head h1');if(!e||!title)return;const name=title.textContent.trim(),rows=[...document.querySelectorAll('.session-card .set-row')],result=e.analyse(name);if(rows.length&&Number.isFinite(result.next)){const changed=e.prefill(name,rows.length);if(changed)rows.forEach(row=>{const input=row.querySelector('input[type="number"]');if(input&&(input.value===''||input.value==null))input.value=result.next})}const old=document.querySelector('.session-card .progress-note');if(old){old.classList.add('progression-smart');const last=result.last?.sets?.map(s=>`${s.weight}kg × ${s.reps}`).join(' / ')||'No previous session';const target=(result.targetReps||[]).join(' / ');old.innerHTML=`<div class="progression-kicker">TODAY'S TARGET</div><b>${result.label||'Progression target'}</b><div class="progression-line"><small>Last time</small><strong>${last}</strong></div>${target?`<div class="progression-line"><small>Aim today</small><strong>${Number.isFinite(result.next)?`${result.next}kg · `:''}${target}</strong></div>`:''}<span>${result.message}</span>`}}
const css=document.createElement('style');css.textContent='.progression-smart{display:flex!important;flex-direction:column;gap:8px;padding:14px!important;border-radius:12px;background:rgba(255,214,51,.07);border:1px solid rgba(255,214,51,.28)}.progression-kicker{font-size:10px;font-weight:800;letter-spacing:.16em;color:#ffd633}.progression-smart>b{font-size:16px;color:#fff}.progression-line{display:flex;justify-content:space-between;gap:12px;padding-top:5px}.progression-line small{color:#888}.progression-line strong{font-size:13px;text-align:right}.progression-smart>span{font-size:12px;line-height:1.4;color:inherit;opacity:.65}html[data-theme="light"] .progression-smart>b{color:#111}';document.head.appendChild(css);const obs=new MutationObserver(()=>requestAnimationFrame(enhance));obs.observe(document.getElementById('app'),{childList:true,subtree:true});requestAnimationFrame(enhance);})();