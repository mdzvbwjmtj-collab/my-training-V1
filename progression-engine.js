(()=>{
const dash=()=>{try{return JSON.parse(localStorage.getItem('myTrainingDashboard')||'null')||{logs:{}}}catch{return{logs:{}}}};
const programme=()=>window.PROGRAM||[];
const completedLogs=()=>Object.entries(dash().logs||{}).filter(([,v])=>v?.done).sort((a,b)=>a[0].localeCompare(b[0]));
const increment=name=>/dumbbell/i.test(name)?1:2.5;
function sessionsFor(name){const out=[];completedLogs().forEach(([key,log])=>{const [date,wid]=key.split('::'),w=programme().find(x=>x.id===wid);if(!w)return;w.exercises.forEach((e,ei)=>{if(e.name!==name)return;const sets=Object.values(log.sets?.[ei]||{}).filter(s=>s?.completed&&Number(s.weight)>0&&Number(s.reps)>0).map(s=>({weight:Number(s.weight),reps:Number(s.reps)}));if(sets.length)out.push({date,wid,sets,bestWeight:Math.max(...sets.map(s=>s.weight)),volume:sets.reduce((n,s)=>n+s.weight*s.reps,0)})})});return out.sort((a,b)=>a.date.localeCompare(b.date))}
function analyse(name){const history=sessionsFor(name);if(!history.length)return{status:'new',message:'First logged session — establish a solid baseline before progressing.',next:null};const last=history.at(-1),prev=history.at(-2);if(!prev)return{status:'baseline',message:'Baseline recorded. Repeat this performance cleanly before increasing load.',next:last.bestWeight};const lastTop=Math.max(...last.sets.map(s=>s.reps)),prevTop=Math.max(...prev.sets.map(s=>s.reps));const weightUp=last.bestWeight>prev.bestWeight,volumeUp=last.volume>prev.volume*1.025,repsUp=last.bestWeight===prev.bestWeight&&lastTop>prevTop;if(weightUp)return{status:'progress',message:`Load progressed from ${prev.bestWeight} kg to ${last.bestWeight} kg. Hold this load until the prescribed reps are secure.`,next:last.bestWeight};if(repsUp||volumeUp){const next=last.bestWeight+increment(name);return{status:'ready',message:`Performance improved at ${last.bestWeight} kg. If all prescribed reps felt controlled, next time try ${next.toFixed(1)} kg.`,next};}return{status:'hold',message:`Hold ${last.bestWeight} kg next time and aim to improve reps or execution before adding load.`,next:last.bestWeight}}
window.MyTrainingProgression={sessionsFor,analyse};
})();